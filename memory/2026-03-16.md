# 2026-03-16

- 修复 Discord 无法连接：根因不是网络或权限，而是当前 `~/.openclaw/openclaw.json` 缺失 `channels.discord` 配置段，导致 OpenClaw 显示 `enabled, not configured`。
- 用户明确要求不要整份回滚旧备份，只允许最小变更恢复。
- 已按守门流程处理：保留当前新版本配置，仅从历史备份提取 Discord 必需配置段合并回当前配置；先启动回滚监控，再用 `config-guard.sh test` 验证，通过后执行 `backup` + `apply`，重启 Gateway，并验证 Discord 状态恢复为 `ON / OK`。
- 结论：此类问题优先采用“最小字段恢复”而不是“整份备份回滚”，避免覆盖近期其他配置改动。
