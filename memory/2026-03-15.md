# 2026-03-15

- 为缓解 Telegram 同会话长任务时“Queued messages while agent was busy”导致的偶发不回问题，已启动回滚监控并备份配置：`/home/lchych/.openclaw/config-backups/auto-rollback-20260315-193704.json`。
- 已生成候选配置 `/tmp/openclaw-responsive.json`，最小改动包括：`agents.defaults.maxConcurrent: 2 -> 4`、新增 `messages.inbound`（Telegram 2000ms 防抖）、新增 `messages.queue.byChannel.telegram = "collect"`、新增 `channels.telegram.timeoutSeconds = 600`、新增 `channels.telegram.streamMode = "partial"`。
- `bash /home/lchych/agents/config-guard/config-guard.sh test -f /tmp/openclaw-responsive.json` 已通过；正式备份生成：`/home/lchych/.openclaw/config-backups/openclaw-20260315-193823.json`。
- `config-guard apply -f /tmp/openclaw-responsive.json` 已发起，但在等待结果时遇到会话压缩；是否最终应用成功，需要在压缩后继续确认运行会话 `marine-canyon` 或直接检查当前生效配置/服务状态。
- 压缩后已确认 `marine-canyon` 会话不存在，且直接读取 `/home/lchych/.openclaw/openclaw.json` 可见改动已生效：`maxConcurrent=4`、`messages.inbound.byChannel.telegram=2000`、`messages.queue.byChannel.telegram="collect"`、`channels.telegram.timeoutSeconds=600`、`channels.telegram.streamMode="partial"`。
- `openclaw status --deep` 后续确认服务健康：`Gateway service` 为 running，健康项中 `Gateway reachable`、`Telegram OK`、`QQ Bot OK`、`Feishu OK`；本次“偶尔不回消息”的最小配置优化已成功落地，剩余仅待实际长任务场景回归验证。
